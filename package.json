{
  "name": "ima-adpcm-decoder",
  "version": "1.0.0",
  "description": "Decode IMA ADPCM WAV Files to AudioContext",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --declaration",
    "prepare": "npm run build",
    "asproduction": "asc assembly/index.ts --target release -O3 --runtime full",
    "asbuild": "asc assembly/index.ts --target debug --debug --runtime full --sourceMap http://localhost:1234/untouched.wasm.map",
    "aswatch": "npm run asbuild && chokidar 'assembly/*.ts' -c 'npm run asbuild'",
    "browser": "parcel src/test/*"
  },
  "keywords": [
    "ima",
    "adpcm",
    "audio",
    "audiocontext",
    "decoder"
  ],
  "author": "Sebastian Langer <sl@scn.cx>",
  "license": "MIT",
  "devDependencies": {
    "assemblyscript": "^0.17.2",
    "chokidar-cli": "^2.1.0",
    "parcel": "^1.12.4",
    "typescript": "^4.1.2"
  },
  "dependencies": {
    "@assemblyscript/loader": "^0.17.2",
    "regenerator-runtime": "^0.13.7",
    "wavefile": "^11.0.0"
  }
}
